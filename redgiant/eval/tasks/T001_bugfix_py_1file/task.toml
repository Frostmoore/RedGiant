id = "T001"
domain = "coding"
prompt = "The test suite fails. Find the bug and fix it without changing the tests."
success_cmd = "pytest -q"
timeout_s = 1200
tags = ["bugfix", "python", "single-file"]
plan_file = "plan.json"
writable_globs = ["calc.py"]

[test_commands]
pytest = ["pytest", "-q"]
