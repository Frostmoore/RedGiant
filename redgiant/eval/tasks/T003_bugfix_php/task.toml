id = "T003"
domain = "coding"
prompt = "The PHP test script fails: VAT totals come out wrong on some amounts. Fix the bug in src/Tax.php without changing test.php."
success_cmd = "php test.php"
timeout_s = 1200
tags = ["bugfix", "php", "single-file"]
plan_file = "plan.json"
writable_globs = ["src/Tax.php"]
requires = ["php"]

[test_commands]
phptest = ["php", "test.php"]
