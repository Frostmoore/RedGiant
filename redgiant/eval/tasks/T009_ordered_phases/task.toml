id = "T009"
domain = "coding"
prompt = "All tests fail with ImportError: the util module does not exist yet. Create util.py with the slug(text) function the tests describe, then fix main.py to use it, without changing the tests."
success_cmd = "pytest -q"
timeout_s = 1500
tags = ["feature", "python", "ordered-phases", "multi-step"]
writable_globs = ["util.py", "main.py"]

[test_commands]
pytest = ["pytest", "-q"]
