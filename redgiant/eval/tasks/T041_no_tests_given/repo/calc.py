"""Tiny calculator helpers (existing code, conventions: typed, docstrings)."""


def add(a: float, b: float) -> float:
    """Somma di due numeri."""
    return a + b


def multiply(a: float, b: float) -> float:
    """Prodotto di due numeri."""
    return a * b
