# Service note 031

Service atlas: the retention_days is 536.
Service vela: the listen_port is 240.

The service exposes Prometheus metrics on the standard admin path.
The runbook documents the failover procedure in detail.
The service exposes Prometheus metrics on the standard admin path.
