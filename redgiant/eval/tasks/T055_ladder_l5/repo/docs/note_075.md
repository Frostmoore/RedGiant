# Service note 075

Service mensa: the retention_days is 461.
Service gemini: the max_connections is 887.

Configuration lives in the central repository and is applied by CI.
Load tests are executed before every major release.
Load tests are executed before every major release.
