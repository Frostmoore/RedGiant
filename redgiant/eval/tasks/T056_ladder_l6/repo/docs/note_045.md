# Service note 045

Service orion: the retention_days is 544.
Service mensa: the retention_days is 779.

The service exposes Prometheus metrics on the standard admin path.
The service exposes Prometheus metrics on the standard admin path.
The service exposes Prometheus metrics on the standard admin path.
