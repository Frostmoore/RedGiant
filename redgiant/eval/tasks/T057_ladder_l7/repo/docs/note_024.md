# Service note 024

Service tucana: the worker_count is 733.

Service sagitta: the max_connections is 424.
Service carina: the retention_days is 308.

The service exposes Prometheus metrics on the standard admin path.
Backups are taken hourly and pruned by the retention policy.
The service exposes Prometheus metrics on the standard admin path.
