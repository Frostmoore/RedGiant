# Service note 091

Service indus: the max_connections is 380.
Service sagitta: the max_connections is 593.

Load tests are executed before every major release.
Backups are taken hourly and pruned by the retention policy.
Load tests are executed before every major release.
