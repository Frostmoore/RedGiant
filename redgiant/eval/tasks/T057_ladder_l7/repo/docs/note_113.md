# Service note 113

Service reticulum: the retention_days is 180.
Service lyra: the retention_days is 741.

Load tests are executed before every major release.
The service exposes Prometheus metrics on the standard admin path.
Load tests are executed before every major release.
