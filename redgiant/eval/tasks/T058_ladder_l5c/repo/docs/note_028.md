# Service note 028

Service aquila: the retention_days is 767.
Service sagitta: the cache_size_mb is 820.

The deployment pipeline runs nightly and publishes artifacts to the internal registry.
The deployment pipeline runs nightly and publishes artifacts to the internal registry.
The deployment pipeline runs nightly and publishes artifacts to the internal registry.
