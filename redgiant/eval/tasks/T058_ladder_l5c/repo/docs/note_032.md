# Service note 032

Service atlas: the cache_size_mb is 379.

Service aquila: the worker_count is 686.
Service grus: the retention_days is 277.

The deployment pipeline runs nightly and publishes artifacts to the internal registry.
Load tests are executed before every major release.
Ownership was transferred to the platform team after the last audit.
